doctype html
html
  head
    title SPS Svalbard Balloon monitor
    style(
    body { margin: 5% 20%; }
    h1,h2,h3 {text-align:center}

    )

    script(
    <script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=false">
    </script><div style="overflow:hidden;height:500px;width:600px;">
    <div id="gmap_canvas" style="height:500px;width:600px;">
    <style>#gmap_canvas img{max-width:none!important;background:none!important}</style>
    <a class="google-map-code" href="http://www.map-embed.com" id="get-map-data">The Circle</a></div>
    <script type="text/javascript"> function init_map(){var myOptions = {zoom:14,center:new google.maps.LatLng(40.805478,-73.96522499999998),mapTypeId: google.maps.MapTypeId.SATELLITE};map = new google.maps.Map(document.getElementById("gmap_canvas"), myOptions);marker = new google.maps.Marker({map: map,position: new google.maps.LatLng(40.805478, -73.96522499999998)});infowindow = new google.maps.InfoWindow({content:"<b>The Circle</b><br/>2880 Broadway<br/> New York" });google.maps.event.addListener(marker, "click", function(){infowindow.open(map,marker);});infowindow.open(map,marker);}google.maps.event.addDomListener(window, 'load', init_map);</script>
    )


  body
    h1 Svalbard Balloon Live Tracker
    h3 Last update: #{last_update_time}

    p A quick note—this is not intended to be a highly ergonomic or polished frontend, but simply an experimental system for giving a rough idea of what's going on in Svalbard during the balloon flights planned.
    hr
    h3 Current Balloon Positions
    div(id = "gmap-canvas")
    hr
    each balloon in balloons
      hr
      h3 Balloon no. #{balloon[0]}
      p Pressure: #{balloon[1]} hPa
      p Temperature
    img(src="img.jpg")
